<p><%= link_to "Back to Merchants near you", merchants_path %></p>
<h3> Merchants names which match: "<%= params[:q] %>"</h3>
<% @merchants.each do |result| %>
	<p><%= link_to( result.name, merchant_path(result.id) )%></p>
<% end %>
